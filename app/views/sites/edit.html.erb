<div class="main-container">
  <h1>Edit Site</h1>

  <div class="txt-center margin-block">
    <%= link_to site_path(@site) do %>
      <%= icon_tag "arrow-left" %> Back to Site
    <% end %> |
    <%= link_to "All Sites", sites_path %>
  </div>

  <%= render 'form', site: @site %>
</div>
